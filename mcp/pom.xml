<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.github.codesapienbe.springvision</groupId>
    <artifactId>spring-vision</artifactId>
    <version>1.0.5</version>
  </parent>

  <artifactId>mcp</artifactId>
  <name>Spring Vision -> MCP Server</name>
  <description>MCP Server for Spring Vision using Spring AI</description>

  <properties>
    <jib-maven-plugin.version>3.4.6</jib-maven-plugin.version>
    <docker.image.name>spring-vision:${project.version}</docker.image.name>
    <spring-boot.version>3.1.3</spring-boot.version>
    <spring-ai.version>1.0.3</spring-ai.version>
  </properties>

  <pluginRepositories>
    <pluginRepository>
      <id>central</id>
      <name>Maven Central</name>
      <url>https://repo.maven.apache.org/maven2</url>
    </pluginRepository>
  </pluginRepositories>

  <repositories>
    <repository>
      <id>central</id>
      <name>Maven Central</name>
      <url>https://repo.maven.apache.org/maven2</url>
    </repository>

    <repository>
      <id>spring-releases</id>
      <name>Spring Releases</name>
      <url>https://repo.spring.io/release</url>
      <releases>
        <enabled>true</enabled>
      </releases>
      <snapshots>
        <enabled>false</enabled>
      </snapshots>
    </repository>
    <repository>
      <id>spring-milestones</id>
      <name>Spring Milestones</name>
      <url>https://repo.spring.io/milestone</url>
      <snapshots>
        <enabled>false</enabled>
      </snapshots>
    </repository>
  </repositories>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.ai</groupId>
        <artifactId>spring-ai-bom</artifactId>
        <version>${spring-ai.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <!-- Spring Vision Core -->
    <dependency>
      <groupId>io.github.codesapienbe.springvision</groupId>
      <artifactId>core</artifactId>
      <version>${project.version}</version>
    </dependency>

    <!-- Spring Boot WebFlux Starter for reactive web -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webflux</artifactId>
    </dependency>

    <!-- Spring Boot Actuator for health checks -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>

    <!-- Jackson for JSON processing -->
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
    </dependency>

    <!-- Logstash Logback Encoder for JSON logging -->
    <dependency>
      <groupId>net.logstash.logback</groupId>
      <artifactId>logstash-logback-encoder</artifactId>
      <version>7.4</version>
    </dependency>

    <!-- Spring AI MCP Server (stdio-only starter) -->
    <dependency>
      <groupId>org.springframework.ai</groupId>
      <artifactId>spring-ai-starter-mcp-server</artifactId>
    </dependency>

    <!-- Spring Boot Test Starter for testing -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- Spring Boot Maven Plugin -->
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <version>${spring-boot.version}</version>
        <configuration>
          <mainClass>io.github.codesapienbe.springvision.mcp.SpringVisionMcpServerApplication</mainClass>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>repackage</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <!-- Add Jib Maven Plugin (Google) so we can build container images without a Dockerfile -->
      <plugin>
        <groupId>com.google.cloud.tools</groupId>
        <artifactId>jib-maven-plugin</artifactId>
        <version>${jib-maven-plugin.version}</version>
        <configuration>
          <!-- Base image must already contain OS-level runtime deps (OpenCV, libglib, etc.) if you need them.
               If you rely on the project Dockerfile to assemble native .so files, continue using that Dockerfile.
               Jib is ideal for fast, reproducible JVM images when a suitable base image is available. -->
          <from>
            <image>eclipse-temurin:21-jre-jammy</image>
          </from>

          <to>
            <image>${docker.image.name}</image>
          </to>

          <!-- JVM flags and environment similar to the Dockerfile -->
          <container>
            <jvmFlags>
              <jvmFlag>--enable-native-access=ALL-UNNAMED</jvmFlag>
              <jvmFlag>-Xms512m</jvmFlag>
              <jvmFlag>-Xmx2g</jvmFlag>
            </jvmFlags>
            <environment>
              <SPRING_PROFILES_ACTIVE>default</SPRING_PROFILES_ACTIVE>
            </environment>
            <!-- For Spring Boot executable jars, Jib will detect and set an appropriate entrypoint (JarLauncher).
                 You can override entrypoint if you prefer to run 'java -jar /app/app.jar'. -->
            <appRoot>/app</appRoot>
          </container>

          <!-- Extra directory allows adding native libs or other files to the image if you place them under src/main/jib -->
          <extraDirectories>
            <paths>
              <path>src/main/jib</path>
            </paths>
          </extraDirectories>
        </configuration>
        <executions>
          <execution>
            <id>docker-build-on-install</id>
            <phase>install</phase>
            <goals>
              <goal>dockerBuild</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

    </plugins>
  </build>
</project>
