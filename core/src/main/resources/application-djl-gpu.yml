# DJL configuration optimized for GPU acceleration
spring:
  vision:
    djl:
      enabled: true
      engine: PyTorch
      confidence-threshold: 0.5

    # Disable legacy backends
    opencv:
      enabled: false
    facebytes:
      enabled: false

# Activate GPU profile
spring:
  profiles:
    active: gpu
